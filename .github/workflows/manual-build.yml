name: Manual build plugin artifacts

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Plugin version, for example 0.1.8"
        required: true
        default: "0.1.7"
        type: string

permissions:
  contents: read

env:
  PLUGIN_ID: api-key-manager
  VERSION_INPUT: ${{ inputs.version }}

jobs:
  build:
    name: Build ${{ matrix.goos }}-${{ matrix.goarch }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            goos: linux
            goarch: amd64
            extension: so
          - runner: windows-latest
            goos: windows
            goarch: amd64
            extension: dll
          - runner: macos-15-intel
            goos: darwin
            goarch: amd64
            extension: dylib
          - runner: macos-14
            goos: darwin
            goarch: arm64
            extension: dylib
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache: false

      - name: Build dynamic library
        shell: bash
        env:
          GOOS_VALUE: ${{ matrix.goos }}
          GOARCH_VALUE: ${{ matrix.goarch }}
          EXTENSION: ${{ matrix.extension }}
        run: |
          set -euo pipefail
          version="$VERSION_INPUT"
          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid plugin version: $version" >&2
            exit 1
          fi

          mkdir -p dist
          output="${PLUGIN_ID}_${version}_${GOOS_VALUE}_${GOARCH_VALUE}.${EXTENSION}"
          CGO_ENABLED=1 GOOS="$GOOS_VALUE" GOARCH="$GOARCH_VALUE" \
            go build -trimpath -buildmode=c-shared \
            -ldflags "-X main.pluginVersion=${version}" \
            -o "dist/${output}" .

      - name: Upload platform artifact
        uses: actions/upload-artifact@v4
        with:
          name: platform-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/*
          if-no-files-found: error
          retention-days: 7

  collect:
    name: Collect downloadable artifacts
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download platform artifacts
        uses: actions/download-artifact@v4
        with:
          path: build-assets
          pattern: platform-*-*
          merge-multiple: true

      - name: Generate checksums
        shell: bash
        run: |
          set -euo pipefail
          cd build-assets
          sha256sum -- * | sort -k2 > checksums.txt
          cat checksums.txt

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: api-key-manager-manual-${{ inputs.version }}
          path: build-assets/*
          if-no-files-found: error
          retention-days: 7
